[project]
name = "ai-restaurant-recommendation-phase5"
version = "0.1.0"
description = "AI Restaurant Recommendation Service (Phase 5 response formatting)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
