[project]
name = "ai-restaurant-recommendation"
version = "0.1.0"
description = "AI Restaurant Recommendation Service (Phase 1 foundation)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
