[project]
name = "ai-restaurant-recommendation-phase3"
version = "0.1.0"
description = "AI Restaurant Recommendation Service (Phase 3 filtering & ranking)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
